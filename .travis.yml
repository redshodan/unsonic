language: python
python:
  - "3.5"
  - "3.6"
  - "3.7-dev" # 3.7 development branch

# command to install dependencies
install:
  - make external
  - pip install -r requirements.txt
  - pip install .

# command to run tests
script: python setup.py tests
